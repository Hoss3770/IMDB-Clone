
<p>
  <strong>Name:</strong>
  <%= @genre.name %>
</p>

<p>
  <strong>Description:</strong>
  <%= @genre.description %>
</p>
<% if admin_signed_in? %>
	<%= link_to 'Edit', edit_genre_path(@genre) %> |
<% end %> 		
<%= link_to 'Back', genres_path %>
